# Secrets — never commit tokens or certificates
.env
.env.*
*.token
*.pem
*.p12

# Template env files (placeholders only — safe to commit)
!.env.sandbox
!.env.staging

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Logs
*.log
